spring:
  application:
    name: test-service-name

  cloud:
#    bus:
#      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false

label: hello